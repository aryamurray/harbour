[package]
name = "harbour-cli"
version = "0.1.0"
edition = "2021"
description = "A Cargo-like package manager and build system for C"
license = "MIT"
repository = "https://github.com/aryamurray/harbour"
keywords = ["c", "build-system", "package-manager"]
categories = ["development-tools", "development-tools::build-utils"]

[[bin]]
name = "harbour"
path = "src/bin/harbour/main.rs"

[lib]
name = "harbour"
path = "src/lib.rs"

[dependencies]
# CLI
clap = { version = "4.5", features = ["derive", "env"] }
clap_complete = "4.5"

# Serialization
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
toml_edit = "0.22"

# Versioning & Resolution
semver = { version = "1.0", features = ["serde"] }
pubgrub = "0.3"

# Graph
petgraph = "0.8"

# Git
git2 = { version = "0.21", features = ["ssh", "https", "cred"] }

# Hashing
sha2 = "0.10"
hex = "0.4"

# Filesystem
walkdir = "2.4"
glob = "0.3"
tempfile = "3.10"
directories = "5.0"
pathdiff = "0.2"

# Error handling
thiserror = "2.0"
anyhow = "1.0"
miette = { version = "7.4", features = ["fancy"] }

# Logging & UI
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
indicatif = "0.18"

# Parallelism
rayon = "1.10"

# Platform detection
which = "8.0"

# URL handling
url = "2.5"

# HTTP client for tarball downloads
reqwest = { version = "0.12", features = ["blocking"] }

# Archive extraction
tar = "0.4"
flate2 = "1.0"

# JSON for compile_commands.json and build plans
serde_json = "1.0"

# Regex for FFI header parsing
regex = "1.10"

# Vcpkg integration
vcpkg = "0.2"

[dev-dependencies]
tempfile = "3.14"
assert_cmd = "2.0"
predicates = "3.1"
# Self-dependency so integration tests under `tests/` can reuse
# `harbour::test_support` (normally only compiled for `#[cfg(test)]` unit
# tests inside the lib itself) via the `test-support` feature below.
harbour-cli = { path = ".", features = ["test-support"] }

[features]
# Exposes `harbour::test_support` outside of `#[cfg(test)]` so that
# integration tests (which link the lib as a normal, non-test dependency)
# can reuse its fixture helpers instead of reinventing them.
test-support = []

[profile.release]
lto = "thin"
codegen-units = 1
strip = true
